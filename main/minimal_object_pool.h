#ifndef MINIMAL_OBJECT_POOL_H
#define MINIMAL_OBJECT_POOL_H

#include <vector>
#include <cstdint>

// Create a minimal object pool for testing
std::vector<uint8_t> createMinimalObjectPool();

#endif // MINIMAL_OBJECT_POOL_H